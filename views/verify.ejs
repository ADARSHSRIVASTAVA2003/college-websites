<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/signup.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="content">
          <!--pinterest logo-->
          <img src="https://i.pinimg.com/originals/d3/d1/75/d3d175e560ae133f1ed5cd4ec173751a.png" alt="pin logo" class="img1" />
          <p class="header">Verify your account</p>

          <p style="margin-top: 20px;">Enter the OTP sent on <span style="color: blue;"><%= phone %></span></p>
          <% if(password){ %>
            <form action="/verify" method="post">
          <% } else{ %>  
              <form action="/verifyForgot" method="post">
          <% } %>
            <input name="username" type="text" value="<%= phone %>" hidden/>
            <input name="otp" type="text" placeholder="OTP" class="detail" required/><br />
            <% if(password){ %>
              <input name="password" type="password" value="<%= password %>" hidden/><br />
            <%} else { %>
              <input name="password" type="text" placeholder="New Password" class="detail" required/><br />
              <input name="confirm" type="text" placeholder="New Password" class="detail" required/><br />
            <% } %> 
          
            
          <input type="submit" class="btn int" value="Verify OTP">
          <!-- <a href="/forgot">forgot password?</a> -->
          </form>
          <% if(notOtpValid){ %>
            <p style="color: red;">Invalid OTP. Please enter correct OTP</p>
          <% } %>
         
          
          <footer>
            <p>
              By continuing, you agree to Pinterest's
              <b>Terms of Service, Privacy policy.</b>
            </p>
            <hr />
          <a href="/login">Already on Pinterest ? LogIn</a>
          </footer>
        </div>
      </div>
  
</body>
</html>